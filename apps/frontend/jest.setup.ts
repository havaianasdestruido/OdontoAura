import '@testing-library/jest-dom';

// TODO: add matchMedia and IntersectionObserver polyfills — components using responsive hooks or RQ devtools will throw in jsdom
// TODO: add afterEach cleanup (jest.runAllTimers / useRealTimers) to prevent React Query internal timers leaking across tests

// TODO: add matchMedia and IntersectionObserver polyfills — components using responsive hooks or RQ devtools will break in jsdom
// TODO: add afterEach cleanup (jest.runAllTimers / useRealTimers) to prevent React Query internal timers leaking across tests
