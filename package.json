{
  "name": "odontoaura",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.25.0",
  "scripts": {
    "postinstall": "pnpm --filter @odontoaura/shared run build",
    "dev": "pnpm --parallel -r run dev",
    "build": "pnpm --parallel -r run build",
    "lint": "pnpm --parallel -r run lint",
    "test": "pnpm --parallel -r run test",
    "test:e2e": "pnpm --parallel -r run test:e2e"
  },
  "engines": {
    "node": ">=22.12.0",
    "pnpm": ">=9"
  }
}
